use rclrs::bindings::*;
use rclrs::context::Context;

#[test]
fn context_struct() {
    // There is no logic here. Nothing need to test.
    // Just confirm create obj is good.
    let _ = Context::new();
}
